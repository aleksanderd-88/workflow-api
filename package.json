{
  "name": "api",
  "version": "1.0.0",
  "description": "API for workflow ui",
  "main": "index.js",
  "author": "Aleksander Dimitrijevic",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "scripts": {
    "start": "node index",
    "dev": "nodemon index",
    "ci": "rm -rf node_modules && yarn install --frozen-lockfile"
  },
  "nodemonConfig": {
    "legacyWatch": true
  }
}
